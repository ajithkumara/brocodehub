services:
  - type: web
    name: tts-app
    env: python
    region: oregon
    plan: free  # or starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 10000  # Render sets this automatically, but good to document